import { Logo } from '@wasabi-cross/ui';

/** Login (mockup 2). El formulario llega con F1-10. */
export function LoginPage(): React.JSX.Element {
  return (
    <main className="wc-root screen">
      <Logo />
      <h1 className="page__title">Entrar</h1>
    </main>
  );
}
