engine-strict=true
# Sin hoisting salvaje: si un paquete usa una dependencia, la declara.
shamefully-hoist=false
# Los peer deps faltantes deben romper la instalación, no aparecer en runtime.
strict-peer-dependencies=false
